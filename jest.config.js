module.exports = {
    testEnvironment: 'node',  // Ensures your tests run in a Node environment
    testPathIgnorePatterns: ['/node_modules/'],  // Avoids running tests on node modules
    verbose: true,  // Provides detailed information during tests
  };
  